/**
 * RBAC Value Objects (VOs).
 * Small, immutable types that normalize and validate input early (CITEXT-like behavior where applicable).
 * Implemented mostly as Java records; provide readable static factories when helpful.
 *
 * @since 0.2.0
 */
package xroigmartin.analyzcorp.rbac.domain.vo;