/**
 * Domain-specific exceptions.
 * Provide precise failure semantics (invariant violations, not-found, uniqueness conflicts)
 * so the application layer can map them to transport-level responses.
 *
 * @since 0.2.0
 */
package xroigmartin.analyzcorp.rbac.domain.exceptions;