/**
 * RBAC domain model: immutable entities and supporting types.
 * Business rules are enforced at construction time and align with the DB schema.
 * No framework dependencies; state changes are expressed via factory methods and withers.
 *
 * @since 0.2.0
 */
package xroigmartin.analyzcorp.rbac.domain.model;