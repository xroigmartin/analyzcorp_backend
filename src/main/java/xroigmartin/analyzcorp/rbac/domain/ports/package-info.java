/**
 * Domain ports (interfaces) for persistence and read models.
 * Pure contracts with no technical dependencies; infrastructure modules provide implementations.
 * Typical usage: repositories for aggregates and a query for effective permissions.
 *
 * @since 0.2.0
 */
package xroigmartin.analyzcorp.rbac.domain.ports;
